import PropTypes from 'prop-types';
import React, { Component } from 'react';
import {
  Text,
  TouchableOpacity,
  View,
  ViewPropTypes,
} from 'react-native';
import styles from './button.style';

export default class BestButton extends React.Component {
    constructor(props) {
        super(props);
    }

}

BestButton.defaultProps = {
    
};

BestButton.propTypes = {

}